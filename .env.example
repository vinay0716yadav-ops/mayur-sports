# Admin Passcode for /admin portal (Default if unset: mayur2026)
ADMIN_PASSWORD=mayur2026

# Optional: Supabase / PostgreSQL database connection (for production on Vercel)
# DATABASE_URL=postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres
